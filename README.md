# Personal dotfiles configuation
## Neovim
Prerequisites
* Install packages.   
  Typescript/language servers  
  `npm i -g typescript typescript-language-server vscode-langservers-extracted cssmodules-language-server`
  
  For ruby/rails  
  `gem install solargraph`  
  `solargraph bundle` - In rails dir
  
  For C#  
  `dotnet tool install --global csharp-ls`
